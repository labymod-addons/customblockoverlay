ColorPickerOverlay {
  background-color: rgba(20, 20, 20, 150);
}

Marker {
  left: 0;
  bottom: 0;
}

ShadeSelector {
  width: 150;
  height: 100;
  top: 20;
  left: 20;

  Marker {
    height: 10;
    width: 10;
  }
}

HueSelector {
  width: 150;
  height: 20;
  top: 130;
  left: 20;

  Marker {
    height: 6;
    width: 6;
  }
}

.color-selector {
  width: 100;
  height: fit-content;
  gradient-width: 64;

  TextField {
    height: 18;
    width: 33;
  }

  Gradient {
  width: 64;
  height: 20;
  }
}

  .whole-selector {
    field-position: right;
    top: 20;
    left: 2;
  Gradient {
  width: 200;
  height: 100;
  }
  }

  .red-selector {
    field-position: right;
    top: 20;
    right: 2;
  }

  .green-selector {
    field-position: right;
    top: 60;
    right: 2;
  }

  .blue-selector {
    field-position: right;
    top: 100;
    right: 2;
  }

  .alpha-selector {
    field-position: right;
  }

  .hue-selector {
    field-position: left;
  }

  .saturation-selector {
    field-position: left;
  }

  .brightness-selector {
    field-position: left;
  }


.color-picker {
  background-color: rgba(20, 20, 20, 150);
  left: 50%;
  alignment-x: center;
  top: 50%;
  alignment-y: center;
  width: 95%;
  height: 200;
}

.color-picker-title {
  top: 0;
  left: 50%;
  alignment-x: center;
}

.selector-list {
  top: 0;
  width: fit-content;
  height: 22;
  space-between-entries: 3;
}